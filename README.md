</> Markdown
# git Auto Pilot

Git automatin test project pilot
git init은 일반 폴더에 .git을 만들어 git 폴더를 만들어. commit 정보, branch 정보, git 설정등 할 수 있는 폴더로 변경
GitHub Repository 연결 -> github의 Repository에서 주소를 확인하고 VSC에서  "git remote add origin https://github.com/사용자이름/git-auto-pilot.git" 으로 연결
"git remote -v" 정상인 것 확인
